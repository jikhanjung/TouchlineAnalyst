# 인수인계 (2026-08-06)

*지금 어디까지 왔고 **바로 무엇을 하면 되는지**. 작업 큐 전체는
`TODOs.md`, 각 결정의 경위는 `devlog/` 가 기준이다. 이 문서는 다음 세션이
**읽자마자 손을 댈 수 있게** 하는 용도라 짧게 유지한다.*

---

## 바로 다음 할 일 — 재분석 배치를 3병렬로 (TODOs #3)

**막고 있던 이유가 전부 없어졌다.** 이제 시작하면 된다.

| 막던 것 | 상태 |
|---|---|
| "far 트랙릿 파편화 튜닝을 먼저" | ✅ 진단 결과 튜닝할 게 없었다 (devlog 104) |
| "2병렬 가능한지 몰라서" | ✅ **3병렬 권장** 실측 (devlog 106) |
| "NVDEC 환경이 없어서" | ✅ 전제 아님 — CPU 디코드로도 됨 (devlog 107) |

### 어떻게 돌리나

- **3병렬**이 무릎. 2병렬은 단독합의 97%(거의 공짜), 3병렬은 2병렬 대비
  **+31%**, 4병렬은 +9% 뿐이라 3을 권한다. 8편 배치 환산 **2.53배**
  (100시간+ → 40시간 아래).
- CPU 디코드로 3병렬 = 총 **5.4/8코어**, 처리량 32.91fps. 문제 없다.
- NVDEC 을 쓰면 35.91fps(**+9%**)에 CPU 3.0코어. **선택 사항** —
  CUDA 빌드 OpenCV 가 필요하고(devlog 105) Windows 프로덕션엔 아직 없다.

### ⚠️ 실행 전 반드시 확인

- **편집(`.ptz.json`)이 있는 영상은 `--far-augment`** (tid 보존).
  없으면 `--reanalyze`. **전체 재분석은 tid 를 갈아치워 기존 편집을
  고아로 만든다.**
- **`--far-augment` 는 원본과 같은 디코드 백엔드**로 돌 것 (devlog 105).
  cudacodec 의 색변환이 CPU 와 미세하게 달라(HSV H1.2/S3.6/V2.6) 한 파일
  안에 두 디코더의 색이 섞이면 팀 분류 군집이 흔들린다.
  섞일 것 같으면 `PYSTITCH_NVDEC=0` 으로 통일하는 게 안전하다.

---

## 이번 세션에서 바뀐 것 (devlog 090~107)

### 코드에 들어간 것

| 변경 | 효과 | devlog |
|---|---|---|
| `--stitch-only` 플래그 | 파노라마만 먼저, 분석은 나중에 | 090 |
| `--persp-k/-m/-widen` | 원근 보정 (기본값은 기존 동작) | 092 |
| remap 맵을 Windows 에서 float32 | 렌더 +11% | 097 |
| 게인 적용을 `cv2.LUT` 로 | 렌더 **+65~72%** (비트 동일) | 098 |
| 워프 버퍼 상주 + `dst=` 직접 쓰기 | 렌더 **+2배** (누적 3.3배) | 100 |
| **원경 tid 네임스페이스 확대 + 가드** | 🔴 **데이터 손상 버그 수정** | 103 |
| 짧은 원경 트랙 필터 (요약 단계) | 트랙릿 **−82%**, 관측 82% 유지 | 104 |
| `core/gpudecode.py` `FrameSource` | 분석 디코드 NVDEC (없으면 폴백) | 105 |
| CI 복구 (10커밋 연속 실패 중이었음) | — | 095 |

### 스티칭 처리량 (같은 소재·같은 출력)

| | 종단 fps | `pano_0002` 환산 |
|---|---|---|
| Windows 최초 | 9.6 | 3.38시간 |
| Windows 현재 | ~19.9 | 1.69시간 |
| **WSL 현재** | **27.5** | **1.18시간** |

**최초 대비 2.86배.** 스티칭 최적화는 종료했다 (devlog 102).

---

## 알아둘 환경 사항

### 스티칭은 WSL 이 1.4배 빠르다

같은 머신·같은 소재에서 Windows 19.9 vs WSL 27.5 fps. 원인은 **Windows 용
opencv-python 휠의 `cv2.remap` 이 CPU 를 더 쓰는 것**(devlog 096·097).
다른 OpenCV 빌드(4.13 PyPI, conda-forge)는 전부 열세라 답이 아니었다.
9p 는 병목이 아니다 (120MB/s vs 필요량 7.5MB/s).

### 🔴 Windows 에서 스티칭할 땐 PATH 를 반드시 앞세울 것

```powershell
$env:PATH = 'D:\ffmpeg\bin;' + $env:PATH   # ffmpeg 8.0.1
```

winget ffmpeg 8.1.2 는 nvenc API 13.1 을 요구하는데 드라이버 591.86 은
13.0 만 준다 → **조용히 libx264 로 폴백**한다 (devlog 090). 드라이버가
610+ 로 올라가기 전까지 유효.

### NVDEC 을 쓰려면 (선택)

CUDA 빌드 OpenCV 필요. WSL 설치 절차는 `scripts/gpu_pipeline_probe.py`
헤더에 있다 (CUDA Toolkit 전체 설치는 불필요 — torch 의 pip nvidia 런타임
+ `nvidia-npp` + micromamba ffmpeg 4.4 prefix). `PYSTITCH_NVDEC=0` 으로 끈다.

구 파노라마는 **h264 6348px** 라 NVDEC h264 한계(4096px)에 걸려 자동으로
CPU 폴백한다. hevc(8192px)로 나가는 신규분은 문제 없다.

---

## 열려 있는 결정

1. **원경 필터 문턱** — `FAR_MIN_OBS_SEC=1.0` 은 0392 분포를 보고 고른
   값이다. 다른 영상에서 `scripts/tracklet_diag.py` 로 확인해볼 것.
   `PYSTITCH_FAR_MIN_SEC` 로 재분석 없이 조정된다.
2. **`conf_far=0.1`** — 짧은 유령 트랙의 **근본 원인**. 필터는 증상 처리다.
   올리면 유령이 애초에 안 생기지만 진짜 원경 선수도 잃을 수 있어,
   재분석 + 회수/손실 측정이 필요하다.
3. **원근 보정 적용 여부** — 육안 최선은 `k=0.30 m=1.30 widen=1.15` 인데
   **원경 정규화 선명도가 −32%** 다 (확대일 뿐 디테일은 안 는다).
   검출엔 무의미하고 사람 눈 가독성만의 문제 (devlog 092).
4. **양재근린공원 경기장 폭** — 촬영 기하 실측이 **79m** 를 함의하는데
   프리셋은 62m 다. 20260801 배치에 92×62m 를 이미 시딩했으니 틀리면
   field 캘리브·레이더 좌표가 어긋난다 (devlog 091).

---

## 측정할 때 조심할 것 (이번 세션에서 반복해 틀린 것)

- **개별 단계 벤치 × N ≠ 파이프라인 N병렬.** 단계는 파이프라인 페이스로
  스로틀된다. 전속력 측정을 외삽해 **"불가능"이라는 결론까지 뒤집힌 적이
  있다** (devlog 101, 107 — 같은 실수 두 번).
- **개별 루프 측정은 페이지 폴트를 숨긴다.** 같은 버퍼를 반복 쓰면 이미
  매핑돼 있다 — 문맥 안에서 재야 한다 (devlog 100).
- **반복 없는 A/B 는 5% 대 차이를 못 가린다.** 이 머신 편차가 조건에 따라
  0.4~7.7% 다 (devlog 099).
- **역산하지 말고 로그를 남길 것.** 정합 시간을 97초로 가정해 fps 를
  잘못 냈다 (실제 119초, devlog 101).
- **통념을 실측할 것.** 고정소수점 맵이 빠르다(Windows 에선 반대),
  NVDEC 이 디코드를 공짜로 만든다(전송이 남으면 6% 뿐).
