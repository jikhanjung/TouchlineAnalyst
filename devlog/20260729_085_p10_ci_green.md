# 085: P10 완료 — CI 그린, 인스톨러 261MB + #8 완료

- 날짜: 2026-07-29
- 종류: 운영 (P10 마감 + P04 #8)

## P10: CI 3라운드 만에 그린

1라운드: `.gitignore` 파이썬 템플릿의 `*.spec` 이 spec 커밋을 막음
('Spec file not found') + build.py 한글 로그가 cp1252 콘솔에서 죽으며
진짜 오류를 가림 + 러너에 ffmpeg 없음.
2라운드(Windows 테스트 4종): **group_directory 가 대소문자 무시 FS 에서
*.MP4/*.mp4 이중 매칭으로 챕터 그룹을 중복 생성하는 실버그**(CI 가
잡아준 실제 사용 버그 — dedupe 수정), PyQt6 importorskip, WSL 전용
테스트 skip 화, PYTHONIOENCODING.
3라운드: **전부 그린** — Tests(ubuntu+windows), Build 8m53s,
`touchlineanalyst-windows-installer` 아티팩트 **261MB** (PitchStitch +
PitchWatch, CPU torch 포함 onedir + InnoSetup).

릴리스 경로: `python scripts/bump_version.py patch --push` → tag →
release.yml 이 테스트 게이트 후 GitHub Release 에 인스톨러+SHA256 첨부.

## P04 #8 (20251102, 언남고) 완료 — 신 검출 스택 정량 확인

| | 샘플 | 검출/샘플 | 원경 타일 비중 | 소요 |
|---|---|---|---|---|
| pano_0389 (107분) | 64,296 | **37.8** | 30% | 스티칭 4.0h + 분석 9.4h |
| pano_0390 (4.4h) | 155,407 | **9.9** | 50% | 스티칭 16.7h + 분석 17.2h |

구 코드 #7 이 1.4/샘플이었다 — 신 스택(원경 타일+이중 conf+트래커
튜닝)의 효과가 실전 규모로 확인됨. OCR 제안 0390 에서 76건. 다음: #9.
