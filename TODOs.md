# 할 일 (작업 큐)

*작업 재개 지점. 완료된 일의 경위는 `devlog/` 가 기준이고, 여기는 **다음에
무엇을 왜 그 순서로** 하는지만 유지한다. 세션 인수인계는 `HANDOFF.md`.*

**마지막 갱신: 2026-08-03** (스티칭 최적화 종료 — devlog 090~102. 다음은 #1)

---

## 지금 순서

### 1. ✅ far 트랙릿 파편화 — 진단 완료, 필터 적용 (devlog 104)

**전제가 틀렸다.** "이어 붙지 않는다"가 아니라 **짧은 유령이 개수를 부풀린
계수 착시**였다. 트래커 파라미터는 건드리지 않는다.

- 기각된 가설: 반경 48px 은 실측 이동(샘플당 0.5px) 기준 **9.4초분**이라
  넉넉하고, 끊긴 뒤 48px 안 재시작은 **1.8%** 뿐이다(재검출 자체가 없음).
  끊긴 뒤 최근접 새 트랙까지 거리가 **간격과 무관하게 평평**(137~179px)해
  그건 선수 간 평균 간격일 뿐 — **넓히면 ID 가 섞인다.**
- 실체: 트랙 절반 이상(≤3샘플)이 관측의 **7.3%** 만 나른다. 1.1초 이상
  지속하는 21.9% 가 관측의 **80.5%**.
- 적용: `FAR_MIN_OBS_SEC=1.0` 을 `analysis_summary`·`team_features` 에서
  거른다 (**비파괴** — 분석 원본 불변, `PYSTITCH_FAR_MIN_SEC` 로 조정).
  0392 실측 원경 트랙 130k→**23.6k**(−82%), 관측은 **82% 유지**.
- **남은 근본 원인은 `conf_far=0.1`** — 필터는 증상 처리다. 올리려면
  재분석이 필요하고 회수/손실을 따로 재야 한다.

### 1.5. 스티칭을 WSL 에서 (코드 수정 0) ← 이것만 적용하면 됨

- **스티칭 최적화는 종료했다** (devlog 102). 결과: 최초 Windows 3.38시간
  → **현재 WSL 1.18시간 (2.86배)**. `pano_0002` 기준.
  종단 fps: Windows 9.6→19.9, WSL 17.1→**27.5**.
- devlog 096: 같은 소재·같은 출력(5976×2270)으로 **Windows vs WSL 1.4배**
  (현재 19.9 vs 27.5). 088 의 17.44fps 도 알고 보니 WSL 값이었다.
- `pano_0002`(3.63시간)를 WSL 에서 돌렸다면 2.2시간. 9p 는 병목 아님.
- **원인 규명됨** (`scripts/bench_stitch_stages.py`): Windows 용
  opencv-python 휠의 `cv2.remap` 이 **프레임당 CPU 를 48% 더 쓴다**
  (184ms vs 124ms, 병렬화 수준은 2.2코어로 동일). GCC/pthreads 빌드 vs
  MSVC/ConcRT 빌드 차이. render 가 임계 경로라 그대로 종단 차이가 된다.
  → 다른 OpenCV 빌드(4.13 PyPI, conda-forge)는 **전부 열세**라 답이
  아니었다. 대신 **맵 타입**이 범인이었다 — devlog 097 에서 Windows 를
  float32 맵으로 바꿔 렌더 +11%/CPU −20%. 그래도 WSL 대비 26% 느린 건
  남는다(MSVC 코드젠 추정, 더 파려면 OpenCV 직접 빌드 필요).

### 2. ✅ 분석 병렬 실측 완료 — **3병렬 권장** (devlog 106)

| | 합계 처리량 | SM | VRAM |
|---|---|---|---|
| 단독 | 14.2 | 52% | 2.1GB |
| 2병렬 | 27.46 (단독합의 **97%**) | 50% | 4.2GB |
| **3병렬** | **35.91** (2병렬 대비 +31%) | 69% | 5.5GB |
| 4병렬 | 39.10 (+9%, 측정 오염) | 78% | 6.7GB |

- 단독 SM 52% 인데 2병렬도 50% — GPU 가 절반은 놀고 있어 두 번째가 틈을
  메운다. 한계는 **VRAM 이 아니라 SM**.
- 8편 배치 환산 **2.53배** → 100시간+ 가 **40시간 아래**로.
- **NVDEC 은 전제가 아니라 선택** (devlog 107 정정) — CPU 디코드로도
  3병렬이 된다 (32.91fps, 총 5.4/8코어). NVDEC 은 **+9.1%**(35.91fps)와
  CPU 5.4→3.0코어. 106 의 "CPU 로는 불가" 는 디코드 단독 벤치를 외삽한
  오류였다.
- ⚠️ 구 파노라마는 **h264 6348px** 라 NVDEC 한계(h264 4096px)에 걸려 못
  탄다. hevc(8192px)로 나가는 신규분은 문제 없음.

### 3. 재분석 배치 (#1~7, #10) ← **지금 바로 할 수 있다** (HANDOFF.md)

- 구 검출 코드 산출물을 신 스택으로. **3병렬 권장** (devlog 106) —
  막고 있던 세 가지(트랙릿 튜닝·병렬성 미확인·NVDEC 환경)가 전부 해소됐다.
- 편집(`.ptz.json`) 있는 영상은 `--far-augment`(tid 보존), 없으면
  `--reanalyze`. **전체 재분석은 tid 를 갈아치워 기존 편집을 고아로 만든다.**

### 4. rotcam 궤적 배치 (32개 큐)

- 수동 단계별로. 구 오케스트레이터(자동 체인)는 유령 프로세스 문제로 폐기.

---

## 스티칭 성능 (20260801 로 측정 완료 — devlog 096)

- **I420 측정 완료** (devlog 096) — 같은 소재 A/B 결과 Windows 에선
  **bgr24 가 5.8% 빠르다**(10.9 vs 10.3). 088 의 "writer 스레드는 놀고
  있으니 공짜" 가정이 틀렸다. WSL 에선 차이가 거의 없다. 되돌릴지는 아래
  WSL 이전과 묶어 판단 (`PYSTITCH_PIPE_FMT` 로 토글 가능).
- **게인 LUT 적용 완료** (devlog 098) — `cv2.multiply` 가 단일 스레드라
  render 의 최대 항목이었다. `cv2.LUT` 로 바꿔 **렌더 +65~72%, 종단
  +29~38%** (비트 동일). 종단: WSL 17.1→**22.0fps**, Windows 10.3→**14.2fps**.
- **파이프 포맷 재측정 완료** (devlog 099) — **WSL 은 I420 이 +47%**
  (22.7 vs 15.4fps). 088 의 I420 변경이 옳았다. Windows 는 +4% 로
  **판정 불가**(실행 간 편차 7.7%). 기본값 I420 유지.
  ⚠️ **096 의 "Windows bgr24 5.8% 우세"는 무효** — 같은 이유로 노이즈였다.
- **⚠️ 측정 방법** — 이 머신의 실행 간 편차가 **7.7%** 다. 5% 대 차이를
  주장하려면 조건당 3회 반복 + 편차 병기. 097 의 종단 +7.3% 도 재확인 대상
  (렌더 단계 in-process 측정은 상대적으로 안전).
- **버퍼 재사용 적용 완료** (devlog 100) — 미설명 13ms 는 **페이지 폴트**
  였다 (매프레임 83MB 할당). 워프 버퍼 상주 + LUT 을 출력 슬라이스에
  `dst=` 직접 쓰기로 **render 3.3배 누적**(WSL 58.0→17.6ms, Windows
  80.7→24.7ms), 종단 +14.5%. 출력 버퍼는 큐 에일리어싱 때문에 일부러
  재사용하지 않았다(4~8% 포기).
- **⚠️ 이제 구속 조건은 총 CPU 예산이다** (devlog 101) — 개별 단계로는
  WSL 이 파이프(39fps), Windows 가 디코드(37fps)지만 **3단 파이프라인이
  단계를 겹쳐 돌리므로 가장 느린 단계가 상한이 아니다.**
  L·R 병렬 디코드는 단독 +22.6% 였는데 **종단 0%** 라 되돌렸다.
  ffmpeg 서브프로세스 NVDEC 도 소프트 대비 **6% 뿐** — 이 경로의 병목은
  디코딩이 아니라 원시 프레임 전송(1,180MB/s)이다.
  → **싼 개선은 소진됐다.** 더 가려면 GPU 상주 경로(SM 59%, 분석과 경합)
  같은 큰 결정이 필요하다.
- **프레임 병렬 렌더 — 철회 검토** (devlog 100). render 가 이제 **7코어**를
  쓰고 병목도 아니다. "유휴 4코어 활용해 1.4~1.7배"라는 원래 근거가
  사라졌다. 총 CPU 가 프레임당 ~250ms 로 8코어 기준 32fps 상당이라
  **이제 개별 단계가 아니라 전체 CPU 예산이 한계**다.
- **스티칭·분석 겹쳐 돌리기** — 자원이 거의 안 겹친다(스티칭 중 SM 9%,
  분석 중 SM 52% → 합 61%). 코드 수정 0.
  **단 이 성질은 현행 CPU 경로에서만 성립한다** — GPU 스티칭은 SM 59% 라
  합이 111% 가 되어 공짜 병렬성을 잃는다 (devlog 096).
- **NVDEC 보류 해제** (devlog 093·094) — CUDA 빌드 OpenCV(cudawarped
  4.13.0.90)로 뚫었다. cudacodec 이 CPU 디코더와 **비트정확**, 시크도 프레임
  정확. 챕터 체인(`scripts/gpu_chapters.py`)까지 붙여 **17,000프레임 완주** —
  **같은 OS·같은 출력 기준 처리량 1.39배**(23.72 vs WSL CPU 17.1fps),
  CPU 는 0.77코어 vs ~4.1코어. 9p 도 병목 아님(120MB/s vs 필요량 7.5MB/s).
  ※ devlog 093·094 의 "2.0배" 는 프로토타입의 순차 루프 기준선 대비값이라
  과대평가였다 — 프로덕션 `export_pano` 는 3단 스레드 파이프라인이다.
  **⚠️ 필수 주의:** `OPENCV_FFMPEG_READ_ATTEMPTS` 기본 4096 에서 GoPro
  챕터가 **조용히 끊긴다**(GP010001 은 30프레임). False 가 EOF 와 구분이 안
  돼 잘린 파노라마가 오류 없이 나온다 — 채택 시 **끊김 검출 필수**.
  남은 것: 색변환(BT.709 풀레인지), 세그먼트 전환, `refine_seam`,
  Windows 적용(CUDA Toolkit 13.1), `render.py` GPU 경로 + 폴백.

---

## 그 밖에 밀린 것

- **양재근린공원 경기장 폭 확인** (devlog 091) — 촬영 기하 실측이 폭
  **79m 근처**를 함의하는데 `venues.json` 프리셋은 62m 다 (삼각대 4m 기준).
  20260801 배치에 92×62m 를 이미 시딩해서, 틀리면 field 캘리브·레이더
  좌표가 어긋난다. 실측/위성지도 확인 → `venues.json` 수정 → 재시딩.
- **원근 보정을 실제로 쓸지 결정** (devlog 092) — 플래그는 넣었다
  (`--persp-k/--persp-m/--persp-widen`, 기본값은 기존 동작). 육안 최선은
  `k=0.30 m=1.30 widen=1.15` 지만 **원경 정규화 선명도가 −32%** 다
  (확대일 뿐 디테일이 늘지 않음). 20260801 은 보정 없이 스티칭 중이라
  적용하려면 재스티칭 2.3시간. 검출용으론 의미 없음 — 네이티브 타일
  경로가 정답이라 **사람 눈 가독성만의 문제**.
- **ffmpeg/드라이버 NVENC 불일치** (devlog 090) — winget ffmpeg 8.1.2 는
  nvenc API 13.1 요구, 드라이버 591.86 은 13.0 → libx264 폴백. 스티칭 전
  `$env:PATH = 'D:\ffmpeg\bin;' + $env:PATH` (8.0.1) 필수. 드라이버가
  610+ 로 올라가기 전까지 유효.
- **#6 스크럽 프록시 미생성** (pano_0374/0375/0376) — GUI 에서 생성.
- **auto-level 발산 근본 원인** — 효창(#11)·양재(0392) 모두 "발산 감지 →
  최소 잔차 해 채택" 경로를 탔다. 파란 보조라인 오염 추정, 미확인.
- **C0012.MP4 손상 확정** — 0391·0392 두 번 다 오디오 추출 실패
  (ffmpeg exit 183). 재복사 여부 판단 필요.
- **파노라마 field_points 캘리브** (Windows GUI 필요) — pano_5316 이 0개라
  P08 실경기 점유율 스팟체크가 막혀 있다.
- **구 h264 파노라마 재인코딩 — 저장 공간 목적으로만** (devlog 107).
  분석 속도 근거로는 손해다 (재인코딩 22~60분 vs 아끼는 분석 14분).
  다만 구 h264 는 **109.8 Mbps** vs 신 hevc **48.1 Mbps** 로 2.3배 헤프다
  — 공간이 필요해지면 그때. 지금 F: 여유 2.8TB.
- **개발 리뷰 권장 순서** — 평가 하네스 → 라벨 내보내기 → 커스텀 YOLO
  파인튜닝 (docs/TouchlineAnalyst_development_review.md 부록).
- **패키징 후속** (devdocs 가이드 검토 결과, 미착수): LICENSE 파일 부재
  (pyproject 는 MIT 선언), `.gitattributes` 부재(WSL2+Windows 드라이브 CRLF
  지뢰), guard_slot + 전역 excepthook, About 다이얼로그, 프로즌 아티팩트
  스모크 게이트, 버전 일관성 테스트.

---

## 참고

- 실행 환경: `source ~/venv/PyStitch360/bin/activate` (시스템 python 엔
  torch 없음). GUI 는 Windows(D:\projects\TouchlineAnalyst).
  **스티칭 배치는 WSL 이 1.66배 빠르다** (devlog 096).
- 원본·산출물: `F:\Pictures` (WSL 은 `/mnt/f`). 9p 는 **대용량 쓰기·랜덤
  시크에서만** 느리다 — 순차 디코드 읽기는 120MB/s 로 여유 (devlog 093).
- 작업 하나 끝나면 **devlog 작성 + commit + push**.
