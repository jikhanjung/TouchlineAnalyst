# 할 일 (작업 큐)

*작업 재개 지점. 완료된 일의 경위는 `devlog/` 가 기준이고, 여기는 **다음에
무엇을 왜 그 순서로** 하는지만 유지한다.*

**마지막 갱신: 2026-08-02** (20260801 스티칭 완료, 처리량 실측 — devlog 090~096)

---

## 지금 순서

### 1. far 트랙릿 파편화 튜닝 ← 여기부터

- **현상 (0392 실측):** 트랙릿 218,879개(원경 tid 800001+ 가 46%),
  세그먼트 241,339개, **길이 중앙값 27프레임 ≈ 0.9초**. 검출은 잘 되는데
  (47.6명/샘플) 이어 붙지 않는다. devlog 081 의 ~1.2초 문제가 규모만 커진 것.
- **왜 먼저인가:** 3번 재분석 배치가 **100시간 이상**의 계산이다. 튜닝 전에
  돌리면 파편화된 트랙릿을 그 시간 들여 다시 만들고, 튜닝 후 또 돌려야 한다.
- **방법:** 0392 짧은 구간(10분)으로 반복 실험. **먼저 현재 상태를
  정량화**(트랙릿 수명 분포, ID 스위치)하고 그 다음 파라미터를 건드린다.
- **후보:** `presets/tracker_pano.yaml` (track_buffer 60, match_thresh,
  with_reid) / `pystitch/core/ptz.py` 의 `_FarTracker` 매칭 반경
  (`pano_w*0.008`)·만료 정책.

### 1.5. 스티칭을 WSL 에서 (코드 수정 0, **1.66배**) ← 가장 싼 개선

- devlog 096: 같은 소재·같은 출력(5976×2270)으로 **Windows 10.3fps vs
  WSL 17.1fps**. 088 의 17.44fps 도 알고 보니 WSL 값이었다.
- `pano_0002`(3.63시간)를 WSL 에서 돌렸다면 2.2시간. 9p 는 병목 아님
  (120MB/s vs 필요량 7.5MB/s). 원인은 미규명(ffmpeg/OpenCV 빌드 추정).

### 2. 분석 2병렬 가능 여부 측정 (30분)

- 분석 중 **GPU 컴퓨트 52%** — 절반이 논다. 재분석 대상 8편은 서로 독립이라
  2병렬로 1.5~1.8배 기대.
- **확인할 것:** VRAM(2080 Ti 11GB 에 YOLO11m 2벌 + 원경 타일 배치가 들어가나)
  과 **CPU 디코드 경합** — 6022×2254 hevc 파노라마 소프트 디코드가 분석 CPU 의
  큰 몫이라 여기서 막힐 수 있다.

### 3. 재분석 배치 (#1~7, #10)

- 구 검출 코드 산출물을 신 스택으로. 1번 튜닝 반영 후, 가능하면 2병렬.
- 편집(`.ptz.json`) 있는 영상은 `--far-augment`(tid 보존), 없으면
  `--reanalyze`. **전체 재분석은 tid 를 갈아치워 기존 편집을 고아로 만든다.**

### 4. rotcam 궤적 배치 (32개 큐)

- 수동 단계별로. 구 오케스트레이터(자동 체인)는 유령 프로세스 문제로 폐기.

---

## 스티칭 성능 (20260801 로 측정 완료 — devlog 096)

- **I420 측정 완료** (devlog 096) — 같은 소재 A/B 결과 Windows 에선
  **bgr24 가 5.8% 빠르다**(10.9 vs 10.3). 088 의 "writer 스레드는 놀고
  있으니 공짜" 가정이 틀렸다. WSL 에선 차이가 거의 없다. 되돌릴지는 아래
  WSL 이전과 묶어 판단 (`PYSTITCH_PIPE_FMT` 로 토글 가능).
- **프레임 병렬 렌더** — 미착수. `Renderer.render()` 는 읽기 전용 맵 + 지역
  출력 버퍼라 스레드 안전. 유휴 4코어 활용해 1.4~1.7배 기대. 반나절.
- **스티칭·분석 겹쳐 돌리기** — 자원이 거의 안 겹친다(스티칭 중 SM 9%,
  분석 중 SM 52% → 합 61%). 코드 수정 0.
  **단 이 성질은 현행 CPU 경로에서만 성립한다** — GPU 스티칭은 SM 59% 라
  합이 111% 가 되어 공짜 병렬성을 잃는다 (devlog 096).
- **NVDEC 보류 해제** (devlog 093·094) — CUDA 빌드 OpenCV(cudawarped
  4.13.0.90)로 뚫었다. cudacodec 이 CPU 디코더와 **비트정확**, 시크도 프레임
  정확. 챕터 체인(`scripts/gpu_chapters.py`)까지 붙여 **17,000프레임 완주** —
  **같은 OS·같은 출력 기준 처리량 1.39배**(23.72 vs WSL CPU 17.1fps),
  CPU 는 0.77코어 vs ~4.1코어. 9p 도 병목 아님(120MB/s vs 필요량 7.5MB/s).
  ※ devlog 093·094 의 "2.0배" 는 프로토타입의 순차 루프 기준선 대비값이라
  과대평가였다 — 프로덕션 `export_pano` 는 3단 스레드 파이프라인이다.
  **⚠️ 필수 주의:** `OPENCV_FFMPEG_READ_ATTEMPTS` 기본 4096 에서 GoPro
  챕터가 **조용히 끊긴다**(GP010001 은 30프레임). False 가 EOF 와 구분이 안
  돼 잘린 파노라마가 오류 없이 나온다 — 채택 시 **끊김 검출 필수**.
  남은 것: 색변환(BT.709 풀레인지), 세그먼트 전환, `refine_seam`,
  Windows 적용(CUDA Toolkit 13.1), `render.py` GPU 경로 + 폴백.

---

## 그 밖에 밀린 것

- **양재근린공원 경기장 폭 확인** (devlog 091) — 촬영 기하 실측이 폭
  **79m 근처**를 함의하는데 `venues.json` 프리셋은 62m 다 (삼각대 4m 기준).
  20260801 배치에 92×62m 를 이미 시딩해서, 틀리면 field 캘리브·레이더
  좌표가 어긋난다. 실측/위성지도 확인 → `venues.json` 수정 → 재시딩.
- **원근 보정을 실제로 쓸지 결정** (devlog 092) — 플래그는 넣었다
  (`--persp-k/--persp-m/--persp-widen`, 기본값은 기존 동작). 육안 최선은
  `k=0.30 m=1.30 widen=1.15` 지만 **원경 정규화 선명도가 −32%** 다
  (확대일 뿐 디테일이 늘지 않음). 20260801 은 보정 없이 스티칭 중이라
  적용하려면 재스티칭 2.3시간. 검출용으론 의미 없음 — 네이티브 타일
  경로가 정답이라 **사람 눈 가독성만의 문제**.
- **ffmpeg/드라이버 NVENC 불일치** (devlog 090) — winget ffmpeg 8.1.2 는
  nvenc API 13.1 요구, 드라이버 591.86 은 13.0 → libx264 폴백. 스티칭 전
  `$env:PATH = 'D:\ffmpeg\bin;' + $env:PATH` (8.0.1) 필수. 드라이버가
  610+ 로 올라가기 전까지 유효.
- **#6 스크럽 프록시 미생성** (pano_0374/0375/0376) — GUI 에서 생성.
- **auto-level 발산 근본 원인** — 효창(#11)·양재(0392) 모두 "발산 감지 →
  최소 잔차 해 채택" 경로를 탔다. 파란 보조라인 오염 추정, 미확인.
- **C0012.MP4 손상 확정** — 0391·0392 두 번 다 오디오 추출 실패
  (ffmpeg exit 183). 재복사 여부 판단 필요.
- **파노라마 field_points 캘리브** (Windows GUI 필요) — pano_5316 이 0개라
  P08 실경기 점유율 스팟체크가 막혀 있다.
- **개발 리뷰 권장 순서** — 평가 하네스 → 라벨 내보내기 → 커스텀 YOLO
  파인튜닝 (docs/TouchlineAnalyst_development_review.md 부록).
- **패키징 후속** (devdocs 가이드 검토 결과, 미착수): LICENSE 파일 부재
  (pyproject 는 MIT 선언), `.gitattributes` 부재(WSL2+Windows 드라이브 CRLF
  지뢰), guard_slot + 전역 excepthook, About 다이얼로그, 프로즌 아티팩트
  스모크 게이트, 버전 일관성 테스트.

---

## 참고

- 실행 환경: `source ~/venv/PyStitch360/bin/activate` (시스템 python 엔
  torch 없음). GUI 는 Windows(D:\projects\TouchlineAnalyst).
  **스티칭 배치는 WSL 이 1.66배 빠르다** (devlog 096).
- 원본·산출물: `F:\Pictures` (WSL 은 `/mnt/f`). 9p 는 **대용량 쓰기·랜덤
  시크에서만** 느리다 — 순차 디코드 읽기는 120MB/s 로 여유 (devlog 093).
- 작업 하나 끝나면 **devlog 작성 + commit + push**.
